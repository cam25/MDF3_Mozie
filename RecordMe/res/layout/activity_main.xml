<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <VideoView
        android:id="@+id/videoView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="142dp" />

    <ImageButton
        android:id="@+id/stopButn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/playButn"
        android:layout_marginLeft="63dp"
        android:layout_toRightOf="@+id/playButn"
        android:src="@drawable/stop" android:contentDescription="@string/todo"/>

    <ImageButton
        android:id="@+id/recordButn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/videoView1"
        android:layout_alignTop="@+id/playButn"
        android:contentDescription="@string/todo"
        android:src="@drawable/record" />

    <ImageButton
        android:id="@+id/playButn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="19dp"
        android:contentDescription="@string/todo"
        android:src="@drawable/play" />

    <ImageButton
        android:id="@+id/openRecorder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/playButn"
        android:layout_toLeftOf="@+id/stopButn"
        android:src="@drawable/ic_launcher" />

</RelativeLayout>
